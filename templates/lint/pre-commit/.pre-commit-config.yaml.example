# https://pre-commit.com/
# Copy to sub-project root as .pre-commit-config.yaml and enable one stack.

repos:
  # --- Biome (JS/TS) ---
  # - repo: local
  #   hooks:
  #     - id: biome-check
  #       name: biome check
  #       entry: npx @biomejs/biome check --write --no-errors-on-unmatched
  #       language: system
  #       types: [javascript, jsx, ts, tsx, json]

  # --- Ruff (Python) ---
  # - repo: https://github.com/astral-sh/ruff-pre-commit
  #   rev: v0.8.4
  #   hooks:
  #     - id: ruff
  #       args: [--fix]
  #     - id: ruff-format

  # --- Prettier + ESLint (JS/TS) ---
  # - repo: https://github.com/pre-commit/mirrors-prettier
  #   rev: v4.0.0
  #   hooks:
  #     - id: prettier
  # - repo: local
  #   hooks:
  #     - id: eslint
  #       name: eslint
  #       entry: npx eslint --fix
  #       language: system
  #       types: [javascript, jsx, ts, tsx]
