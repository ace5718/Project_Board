# Python module skeleton

Rename this folder to your module name (snake_case), then place under the package path.

Suggested layout:

```text
<module>/
  __init__.py
  service.py
  schemas.py
  tests/
```

Naming:

- Files/folders: `snake_case`
- Keep modules focused and small (soft 300-line guideline)
